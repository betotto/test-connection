{
  "name": "test-connection",
  "version": "0.0.1",
  "description": "Testing cloud environment",
  "main": "index.js",
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "test": "echo hello",
    "start": "node app.js"
  },
  "author": "betotto",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "mysql": "^2.16.0"
  },
  "cloud-repo-tools": {
    "requiresKeyFile": true,
    "requiresProjectId": true,
    "test": {
      "build": {
        "requiredEnvVars": [
          "SQL_USER",
          "SQL_PASSWORD",
          "SQL_DATABASE",
          "SQL_PORT",
          "INSTANCE_CONNECTION_NAME"
        ]
      }
    }
  }
}
